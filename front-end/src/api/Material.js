import axios from "../config/axios.js"

export default class MaterialAPI {
    index(request, response) {

        // const materials = MaterialDAO.index()

        response.status(200).json("sd")
    }
}