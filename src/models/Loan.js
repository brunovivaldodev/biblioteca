
export default class Loan {
    constructor(identifier, user, material, amount) {
        this.identifier = identifier
        this.user = user
        this.material = material
        this.amount = amount
    }
}
